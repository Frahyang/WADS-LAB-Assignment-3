version: "3.8"

services:
  backend:
    image: juwono136/todolist-backend:latest
    restart: always
    environment:
      PORT: ${5000}
      CONNECTION_URL: ${mongodb+srv://frahyang2005:LRFtgFHjTXIWmHga@cluster-todolistmern.o0qlg.mongodb.net/?retryWrites=true&w=majority&appName=cluster-todolistmern}
      REFRESH_TOKEN_SECRET: ${sigma}
      EMAIL_USER: ${suss99847@gmail.com}
      EMAIL_PASSWORD: ${syou pgih hvtm icjb}
    ports:
      - "3096:3096"

  frontend:
    image: juwono136/todolist-frontend:latest
    restart: always
    depends_on:
      - backend
    ports:
      - "3095:3095"